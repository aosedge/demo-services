# Offline inference dependencies for models
transformers==4.44.0
accelerate==0.33.0
bitsandbytes==0.44.1
safetensors==0.4.3
torch==2.2.1
# If bitsandbytes (GPU 4-bit) not supported on target, set PHI2_4BIT=0 and keep torch CPU.

