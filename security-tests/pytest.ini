[pytest]
testpaths = bootstrap cloud
markers =
    bootstrap: needs a VM plus a one-time cloud provisioning, then inspects locally
    cloud: needs live AosCloud interaction for the whole test
    destructive: deprovisions the unit or destroys volumes
addopts = -ra --strict-markers
log_cli_level = INFO
